# Diário — 2026-05-20

## O que foi feito

### Migration sources_and_assets — aplicada no Supabase

- Arquivo `001_sources_and_assets.sql` (sem timestamp) renomeado para `20260520000001_sources_and_assets.sql` seguindo padrão das demais migrations
- `content_packages` renomeado para `source_packages` dentro da migration para evitar colisão com a tabela `content_packages` do Sprint 2 (Agente 2) — dois sistemas distintos com nomes diferentes
- Tabelas criadas no banco: `sources`, `source_packages`, `content_assets`
- Colunas `brands.voice` e `brands.audience`, `posts.source_id` e `posts.asset_id` — já existiam (migration anterior havia rodado parcialmente)
- Bucket privado `sources` criado via MCP Supabase com policy owner-only
- Branch: `feat/sources-and-assets-migration`

### Fix 404 — botão "Ver post" em packages

- Causa: `packages/[id]/page.tsx:393` fazia `router.push('/dashboard/posts/${pkg.post_id}')` — rota inexistente
- Fix: mudado para `router.push('/dashboard/posts')` (listagem, onde o post aparece)
- Build passou limpo
- Branch: `fix/post-404`

### Diagnóstico de progresso — Sprint 1 do sistema de agentes

Investigação completa do estado real do projeto vs plano original de 8 passos do Sprint 1:

| Passo | Status |
|-------|--------|
| 1. Cadastrar IrrigaAgro como brand | ✅ |
| 2. SQL 1 — tabelas brand_plans, content_ideas, agent_runs | ✅ |
| 3. SQL 2 — seed IrrigaAgro com tom/pilares/plano | ✅ |
| 4. Chave Gemini configurada | ✅ |
| 5. Secret no Supabase | ✅ |
| 6. Deploy Agente 1 (Strategist) | ✅ |
| 7. Testar — 7 ideias geradas | ✅ (7 rows em content_ideas) |
| 8. Conferir e ajustar tom | ✅ assumido |

Sprint 1 **100% concluído**. Projeto está em Sprint 3 Fase 3 (renderer @vercel/og).

### Extração de conhecimento → Vault Obsidian

Notas criadas/atualizadas no vault:
- `50-Meus-projetos/publik/_início.md` — homepage do projeto
- `20-Como-funciona/Bugs-que-me-ferraram/bug-supabase-create-table-if-not-exists.md` — CREATE TABLE IF NOT EXISTS silencioso
- `20-Como-funciona/Como-eu-faço/retry-result-pattern.md` — padrão RetryResult<T> sem throw
- `20-Como-funciona/Como-eu-faço/llm-erros-retryable.md` — 503/429/529 fazem retry, outros falham direto
- `20-Como-funciona/Como-eu-faço/react-key-pattern-dialogs.md` — key={id} reseta estado de dialogs
- `50-Meus-projetos/publik/Como-as-coisas-acontecem/fluxo-ideia-pacote-post.md` — fluxo end-to-end Agente 1→2→Post

## Decisões técnicas

- `source_packages` em vez de `content_packages` na feature Fontes — dois sistemas não podem compartilhar nome de tabela mesmo que conceitualmente similares
- Itens 3 e 4 da extração promovidos para `20-Como-funciona/Como-eu-faço/` (global) em vez de ficarem em `publik/Por-que-fiz-assim/` — retry pattern e classificação de erros LLM são transversais a qualquer projeto

## Problemas resolvidos

- **Migration parcialmente aplicada**: `001_sources_and_assets.sql` havia rodado antes (colunas em brands e posts já existiam, tabela `sources` também). Solução: checar banco antes de reaplicar, aplicar só o delta (`source_packages` + bucket)
- **Colisão de nomes**: `content_packages` existia no Sprint 2 com schema diferente. Renomear na migration nova evita conflito sem precisar mexer no sistema existente

## Pendências

- Sprint 3 Fase 4: Wizard de Brand multi-step + sugestão de kit por IA
- Sprint 3 Fase 5: Agente 3 Designer
- Sprint 3 Fase 6: UI completa packages + upload de fotos
- Deploy `agent-1-strategist` com retry (pendente desde sessão anterior)
- UI para criar/editar `brand_plans` (hoje só via seed SQL)
- pg_cron — agendamento automático dos agentes
- Redis/Upstash + worker BullMQ (feature Fontes)
